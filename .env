# Optional RINKDESK_* overrides. Empty by default; defaults live in rinkdesk/.env
# (baked into the image). Add a line here to change one on this host.
RINKDESK_LIVE_REFRESH_SECONDS=1
RINKDESK_LIVE_CACHE_SECONDS=1